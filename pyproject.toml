[project]
name = "sentinel"
version = "0.1.0"
description = "SENTINEL — Smart Grid Anomaly Detection & Forecasting Dashboard"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.115.0",
    "pandas==2.2.2",
    "uvicorn==0.32.0",
    "python-multipart==0.0.9",
]
